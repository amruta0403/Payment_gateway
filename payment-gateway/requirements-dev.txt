# Development & testing tools — install with: pip install -r requirements-dev.txt
# These are NOT service dependencies; they run on your host machine or CI.

# Load testing
locust>=2.20.0

# Integration test HTTP client
httpx[http2]>=0.27.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
anyio[trio]>=4.3.0

# Error tracking SDK (same version used inside services)
sentry-sdk[fastapi,sqlalchemy]>=2.0.0

# Code quality
ruff>=0.4.0
mypy>=1.10.0
